{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "go",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "args": ["-inputFile=./swagger.json", "-outputDirectory=./output", "-regex=.*\/{version}\/{action}", "-language=typescript"],
      "request": "launch",
    }
  ]
}
